# Dev: connect directly to backend (bypasses Vite proxy which buffers SSE)
# Use 127.0.0.1 not localhost to avoid IPv6 resolution mismatch
# Use empty string to fall back to Vite proxy (avoids CORS issues with Docker)
VITE_API_URL=
